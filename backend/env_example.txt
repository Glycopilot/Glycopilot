# Environment variables example
# Copy this file to .env and fill in your values

# Database
DB_NAME=glycopilot_db
DB_USER=glycopilot_user
DB_PASSWORD=glycopilot_password
DB_HOST=localhost
DB_PORT=3306
# Use require for AWS RDS PostgreSQL. Leave empty for local Docker PostgreSQL.
DB_SSLMODE=

# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081,http://localhost:8082
CORS_ALLOWED_ORIGIN_REGEXES=^http://localhost:\d+$,^http://127\.0\.0\.1:\d+$
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:8081,http://localhost:8082
SECURE_SSL_REDIRECT=False
USE_X_FORWARDED_HOST=True
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_CROSS_ORIGIN_OPENER_POLICY=

# API
API_VERSION=1.0.0

# Email / 2FA
# If SMTP_* is not fully configured, emails are written to application logs.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@glycopilot.com

# AI Microservice
AI_SERVICE_URL=http://localhost:8001
AI_SERVICE_TOKEN=dev_secret

